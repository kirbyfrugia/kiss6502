# Atari DOS 2.5

`DOS.SYS` and `DUP.SYS` are not bundled. Provide your own DOS 2.5 `DOS.SYS` and
`DUP.SYS` in this directory (gitignored), or set `ATARI_DOS_DIR` to a directory
that has them. They must be DOS 2.5.

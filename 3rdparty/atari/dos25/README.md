# DOS 2.5 boot files

These are not bundled. Drop Atari DOS 2.5 `DOS.SYS` and `DUP.SYS` in this
directory, or point `ATARI_DOS_DIR` at a directory that has them:

    make atari ATARI_DOS_DIR=/path/to/dos25
